name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install ImageMagick
        run: sudo apt-get update && sudo apt-get install -y imagemagick

      - name: Stamp Git Hash onto Debug Icons
        run: |
          chmod +x scripts/badge-debug-icons.sh
          ./scripts/badge-debug-icons.sh

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24

      - name: Install dependencies
        run: npm ci || npm install

      - name: Build Web App
        env:
          VITE_API_BASE_URL: https://sohay-app.onrender.com/api
          VITE_ORGANISATION_NAME: West Bengal Forum for Mental Health
          VITE_APP_TIMEZONE: Asia/Kolkata
        run: npm run build

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Android Platform Sync
        run: npx cap sync android

      - name: Build Android APK
        run: |
          cd android
          ./gradlew assembleDebug

      - name: Rename APK
        run: |
          mv android/app/build/outputs/apk/debug/app-debug.apk \
             android/app/build/outputs/apk/debug/sohay-app.apk

      - name: Upload APK
        uses: actions/upload-artifact@v5
        with:
          name: sohay-app
          path: frontend/android/app/build/outputs/apk/debug/sohay-app.apk
