import {google} from 'googleapis'; import {Readable} from 'node:stream'; import {env} from '../config/env.js';
export async function uploadVideo({fileName,buffer}){if(!env.googleClientId||env.googleClientId==='REPLACE_ME'||!env.googleRefreshToken||env.googleRefreshToken==='REPLACE_ME'){console.warn(`[DRIVE] OAuth not configured for target ${env.googleTargetEmail}; skipping`);return null}const auth=new google.auth.OAuth2(env.googleClientId,env.googleClientSecret,env.googleRedirectUri);auth.setCredentials({refresh_token:env.googleRefreshToken});const drive=google.drive({version:'v3',auth});const out=await drive.files.create({requestBody:{name:fileName,parents:env.googleFolderId&&env.googleFolderId!=='REPLACE_ME'?[env.googleFolderId]:undefined},media:{mimeType:'video/webm',body:Readable.from(buffer)},fields:'id,name,webViewLink'});return out.data}
