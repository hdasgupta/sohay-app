import {pool} from '../config/db.js';
import {findUserByEmail,updatePasswordByEmail} from '../../scripts/sql/generalQueries.js';
import {createPatientUser,createPatientRole} from '../../scripts/sql/patientQueries.js';
import {consumeCaptcha} from '../services/captchaService.js';
import {verifyOtpVerification,hashPassword} from '../services/authService.js';
import {allowedSexes,strongPassword} from '../utils/validation.js';
export async function registerPatient(req,res){const b=req.body;const email=String(b.email||'').trim().toLowerCase();if(!consumeCaptcha(b.captchaId,b.captchaAnswer))return res.status(400).json({message:'Invalid captcha.'});if(!allowedSexes.has(b.sex))return res.status(400).json({message:'Invalid sex value.'});if(!/^\d{4}-\d{2}-\d{2}$/.test(String(b.dateOfBirth||''))||String(b.dateOfBirth)>new Date().toISOString().slice(0,10))return res.status(400).json({message:'Invalid date of birth.'});if(!strongPassword.test(String(b.password||'')))return res.status(400).json({message:'Password does not meet all password rules.'});if(b.password!==b.confirmPassword)return res.status(400).json({message:'Password and confirmation do not match.'});verifyOtpVerification(b.otpVerificationToken,email,'register');const exists=await pool.query(findUserByEmail,[email,'admin','patient','doctor',1]);if(exists.rowCount)return res.status(409).json({message:'Email is already registered.'});const client=await pool.connect();try{await client.query(beginTransaction);const u=await client.query(createPatientUser,[String(b.name).trim(),email,await hashPassword(b.password),false]);await client.query(createPatientRole,[u.rows[0].id,b.sex,b.dateOfBirth,String(b.contactNumber).trim()]);await client.query(commitTransaction);console.log('[AUTH] patient created',email);res.status(201).json({message:'Patient registration completed successfully.'})}catch(e){await client.query(rollbackTransaction);throw e}finally{client.release()}}
export async function resetPassword(req,res){const b=req.body;const email=String(b.email||'').trim().toLowerCase();if(!consumeCaptcha(b.captchaId,b.captchaAnswer))return res.status(400).json({message:'Invalid captcha.'});if(!strongPassword.test(String(b.password||'')))return res.status(400).json({message:'Password does not meet all password rules.'});if(b.password!==b.confirmPassword)return res.status(400).json({message:'Password and confirmation do not match.'});verifyOtpVerification(b.otpVerificationToken,email,'reset');const exists=await pool.query(findUserByEmail,[email,'admin','patient','doctor',1]);if(!exists.rowCount)return res.status(404).json({message:'Email does not exist in the user database.'});await pool.query(updatePasswordByEmail,[await hashPassword(b.password),email]);console.log('[AUTH] password reset',email);res.json({message:'Password reset completed successfully.'})}
