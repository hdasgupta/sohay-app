import express from 'express'; import cors from 'cors'; import helmet from 'helmet'; import rateLimit from 'express-rate-limit'; import {env} from './config/env.js'; import general from './routes/generalRoutes.js'; import auth from './routes/authRoutes.js'; import admin from './routes/adminRoutes.js'; import patient from './routes/patientRoutes.js'; import doctor from './routes/doctorRoutes.js'; import {requestId} from './middleware/requestId.js'; import {notFound,errorHandler} from './middleware/errorHandler.js';
export const app=express();app.set('trust proxy',1);app.use(requestId);app.use(helmet({crossOriginEmbedderPolicy:false}));app.use(cors({origin:env.frontendUrl,methods:['GET','POST','PUT','PATCH','DELETE','OPTIONS'],allowedHeaders:['Content-Type','Authorization']}));app.use(rateLimit({windowMs:900000,limit:300,standardHeaders:true,legacyHeaders:false}));app.use((req,res,next)=>{if(req.path==='/api/doctor/recordings'&&req.method==='POST'){let size=0;let body=[];req.on('data',c=>{size+=c.length;if(size>150*1024*1024){req.destroy(new Error('Recording too large.'));return}body.push(c)});req.on('end',()=>{req.body=Buffer.concat(body);next()});return}express.json({limit:'2mb'})(req,res,next)});app.get('/',(req,res)=>res.json({service:'appointment-backend',status:'ok'}));app.use('/api',general);app.use('/api/auth',auth);app.use('/api/admin',admin);app.use('/api/patient',patient);app.use('/api/doctor',doctor);app.use(notFound);app.use(errorHandler);
