import {Router} from 'express'; import rateLimit from 'express-rate-limit'; import {health,captcha,login,requestOtp,verifyOtpCode} from '../controllers/generalController.js';
const r=Router();const authLimit=rateLimit({windowMs:900000,limit:30,standardHeaders:true,legacyHeaders:false,message:{message:'Too many authentication attempts. Please try again later.'}});r.get('/health',health);r.get('/captcha',captcha);r.post('/login',authLimit,login);r.post('/otp/request',authLimit,requestOtp);r.post('/otp/verify',authLimit,verifyOtpCode);export default r;
