import {pool} from '../src/config/db.js'; import {hashPassword} from '../src/services/authService.js'; import {findUserByEmail} from './sql/generalQueries.js'; import {createUser,createAdminRole} from './sql/adminQueries.js';
export async function seedAdmin(){const email='wbffmh@gmail.com';const exists=await pool.query(findUserByEmail,[email,'admin','patient','doctor',1]);if(exists.rowCount)return;const r=await pool.query(createUser,['WBFFMH Administrator',email,await hashPassword('Admin@12345'),false]);await pool.query(createAdminRole,[r.rows[0].id]);console.log('[SEED] default admin created')}
