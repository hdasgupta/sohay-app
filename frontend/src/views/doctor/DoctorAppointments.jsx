import {useEffect,useState} from 'react'; import {api,getErrorMessage} from '../../api/client'; import {useApp} from '../../context/AppContext'; import Pagination from '../../components/Pagination'; import './Doctor.css';
export default function DoctorAppointments(){const[items,setItems]=useState([]);const[page,setPage]=useState(1);const[perPage,setPerPage]=useState(5);const{notify}=useApp();useEffect(()=>{api.get('/doctor/appointments').then(r=>setItems(r.data.items)).catch(e=>notify('error',getErrorMessage(e)))},[]);return <div><div className='page-title'><div><h1>Appointment List</h1><div className='muted'>Appointments are read-only for doctors.</div></div></div><Pagination items={items} page={page} setPage={setPage} perPage={perPage} setPerPage={setPerPage}/><div className='cards'>{items.slice((page-1)*perPage,page*perPage).map(a=><div className='card' key={a.id}><h3>{a.patient_name}</h3><div>{a.patient_email}</div><div><b>{String(a.appointment_date).slice(0,10)}</b> · {String(a.start_time).slice(0,5)}-{String(a.end_time).slice(0,5)}</div><div>Date of birth: {String(a.date_of_birth).slice(0,10)}</div><div>Status: {a.status}</div></div>)}</div><Pagination items={items} page={page} setPage={setPage} perPage={perPage} setPerPage={setPerPage}/></div>}
