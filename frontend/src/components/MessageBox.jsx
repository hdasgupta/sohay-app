import {useEffect} from 'react'; import './MessageBox.css'; export default function MessageBox({message,onClose}){useEffect(()=>{if(!message)return;const id=setTimeout(onClose,message.duration||10000);return()=>clearTimeout(id)},[message,onClose]);if(!message)return null;const icon={info:'ℹ️',success:'✅',warning:'⚠️',error:'❌'}[message.type]||'ℹ️';return <div className={`message-box ${message.type}`}><span>{icon}</span><span className='message-text'>{message.text}</span><button onClick={onClose}>×</button><div className='message-progress' style={{animationDuration:`${message.duration||10000}ms`}}/></div>}
