import {useEffect,useMemo,useState} from 'react'; import './Datepicker.css';
const names=['Sun','Mon','Tue','Wed','Thu','Fri','Sat'];
const iso=d=>d?`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`:'';
export default function Datepicker({value,startDate,endDate,enabledWeekDays=[0,1,2,3,4,5,6],onDateSelect}){const start=new Date(`${startDate}T00:00:00`),end=new Date(`${endDate}T00:00:00`);const[selected,setSelected]=useState(value?new Date(`${value}T00:00:00`):null);const[open,setOpen]=useState(false);const[month,setMonth]=useState(value?new Date(`${value}T00:00:00`):start);useEffect(()=>{setSelected(value?new Date(`${value}T00:00:00`):null);if(value)setMonth(new Date(`${value}T00:00:00`))},[value]);const days=useMemo(()=>{const first=new Date(month.getFullYear(),month.getMonth(),1);const last=new Date(month.getFullYear(),month.getMonth()+1,0);const a=Array(first.getDay()).fill(null);for(let i=1;i<=last.getDate();i++)a.push(new Date(month.getFullYear(),month.getMonth(),i));return a},[month]);const choose=d=>{if(!d||d<start||d>end||!enabledWeekDays.includes(d.getDay()))return;setSelected(d);setOpen(false);onDateSelect?.(iso(d))};return <div className='datepicker'><button type='button' className='datepicker-input' onClick={()=>setOpen(v=>!v)}>{selected?iso(selected):'Select date'}</button>{open&&<div className='calendar'><div className='calendar-head'><button type='button' onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()-1,1))}>‹</button><b>{month.toLocaleString(undefined,{month:'long',year:'numeric'})}</b><button type='button' onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()+1,1))}>›</button></div><div className='week-row'>{names.map(n=><span key={n}>{n}</span>)}</div><div className='day-grid'>{days.map((d,i)=>{const disabled=!d||d<start||d>end||!enabledWeekDays.includes(d.getDay());return <button type='button' disabled={disabled} className={selected&&d&&iso(selected)===iso(d)?'chosen':''} onClick={()=>choose(d)} key={i}>{d?.getDate()||''}</button>})}</div></div>}</div>}
