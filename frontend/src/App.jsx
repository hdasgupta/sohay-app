import {BrowserRouter,Navigate,Route,Routes} from 'react-router-dom'; import {useApp} from './context/AppContext'; import MessageBox from './components/MessageBox'; import Layout from './components/Layout'; import Login from './views/auth/Login'; import RegisterPatient from './views/auth/RegisterPatient'; import ResetPassword from './views/auth/ResetPassword'; import AddDoctor from './views/admin/AddDoctor'; import DoctorList from './views/admin/DoctorList'; import RescheduleAppointment from './views/admin/RescheduleAppointment'; import BookAppointment from './views/patient/BookAppointment'; import FamilyManagement from './views/patient/FamilyManagement'; import PatientAppointments from './views/patient/PatientAppointments'; import GeneratePrescription from './views/doctor/GeneratePrescription'; import DoctorAppointments from './views/doctor/DoctorAppointments'; import VideoRoom from './views/doctor/VideoRoom'; import './styles.css';
function Protected({role,children}){const{session}=useApp();if(!session)return <Navigate to='/login' replace/>;if(role&&session.user.role!==role)return <Navigate to={`/${session.user.role}`} replace/>;return children}
function Public({children}){const{session}=useApp();return session?<Navigate to={`/${session.user.role}`} replace/>:children}
export default function App(){const{message,setMessage}=useApp();return <BrowserRouter><MessageBox message={message} onClose={()=>setMessage(null)}/><Routes><Route path='/login' element={<Public><Login/></Public>}/><Route path='/register' element={<Public><RegisterPatient/></Public>}/><Route path='/reset-password' element={<Public><ResetPassword/></Public>}/><Route element={<Protected><Layout/></Protected>}><Route path='/video/:roomId' element={<VideoRoom/>}/></Route><Route path='/admin' element={<Protected role='admin'><Layout/></Protected>}><Route index element={<AddDoctor/>}/><Route path='doctors' element={<DoctorList/>}/><Route path='doctors/:id/edit' element={<AddDoctor/>}/><Route path='reschedule' element={<RescheduleAppointment/>}/></Route><Route path='/patient' element={<Protected role='patient'><Layout/></Protected>}><Route index element={<BookAppointment/>}/><Route path='family' element={<FamilyManagement/>}/><Route path='appointments' element={<PatientAppointments/>}/></Route><Route path='/doctor' element={<Protected role='doctor'><Layout/></Protected>}><Route index element={<GeneratePrescription/>}/><Route path='appointments' element={<DoctorAppointments/>}/></Route><Route path='*' element={<Navigate to='/login' replace/>}/></Routes></BrowserRouter>}
